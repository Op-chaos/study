#include<stdio.h>
#include<string.h>
// 未完成

int main()
{
    char input_s[1000];
    int x,y;
    char *curr;
    while(gets(input_s) != NULL) {
        curr = strchr(input_s, ';');
    }
    return 0;
}